#include <stdio.h>

/**
 * Use the function puts
 * You are not allowwed to use printf
 * The program should end with the value 0
 */

int main (void)
{
		puts("Programming is like building a multilingual puzzle");
		return(0);
}
